# AGENTS

绘梦（Masterpiece Timer）。改界面先读 `design.md`。未获明令不提交、不推送 Git。

## 上色分割线只是概念

对话里说的「线」只是灰 / 彩分界的指代，**界面上不得画出这根线**。

禁止：

- 屏心竖线（例如 `centerLine`）
- 叠在画上的 boundary / 描边 / 发光来标出分界
- 为了「看清线」而加任何可见标记

分界只靠两层同图：底灰度、面彩色，`clip-path` 裁出已上色区域。

规则：

- 线左侧未上色，右侧已上色；上色自右向左推进。
- **适配**（高度铺满）：画比屏宽时，虚拟线尽量留在屏心，**画跟着线平移**，不是一开场就让画一直动。
  - 已上色还铺不满右半屏：画不动，等线从右缘走到屏心。
  - 线到屏心之后：钉住，画平移。
  - 未上色铺不满左半屏：画停在左齐，线继续往左走完。
- 整幅能放下时只居中，线在画上走，不钉屏心。
- **全览**：contain 整幅；手卷从右起首平移到左题跋。
- 手卷跳过右端包首 / 空绫（`HANDSCROLL_START_INSET`），从真正起首起算。

改相机或上色只动 `src/components/artworkFrame.ts` 与 `ArtworkReveal.*`，并跑 `src/components/artworkFrame.test.ts`。
